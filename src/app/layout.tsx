import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Rigid Body Sandbox — Custom 2D Physics Engine",
  description: "A from-scratch 2D rigid-body physics sandbox built with HTML5 Canvas and TypeScript. SAT collision detection, impulse-based resolution, friction, restitution — zero physics libraries.",
  keywords: ["physics engine", "2D physics", "rigid body", "SAT", "collision detection", "canvas", "TypeScript", "impulse solver"],
  authors: [{ name: "Z.ai" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
  openGraph: {
    title: "Rigid Body Sandbox",
    description: "Custom 2D physics engine — SAT collision, impulse solver, no libraries.",
    url: "https://chat.z.ai",
    siteName: "Z.ai",
    type: "website",
  },
  twitter: {
    card: "summary_large_image",
    title: "Rigid Body Sandbox",
    description: "Custom 2D physics engine — SAT collision, impulse solver, no libraries.",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
